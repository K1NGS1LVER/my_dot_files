
# --- Ghostty Configuration ---

# --- Fonts ---
font-family = "JetBrainsMono Nerd Font"
font-size = 16
font-feature = calt
font-feature = liga

# Adjust character spacing
adjust-cell-width = 0
adjust-cell-height = 15%

# Alternative fallback fonts for symbols/icons
font-family-bold = "JetBrainsMono Nerd Font"
font-family-italic = "JetBrainsMono Nerd Font"

# --- Theme & Colors ---

theme = Kanagawa Wave
background-opacity = 0.75
background-blur-radius = 25

# --- Premium Aesthetic Tweaks ---
cursor-color = #f5e0dc
cursor-style = bar

# --- Window ---
window-padding-x = 0
window-padding-y = 0
window-decoration = true 
macos-titlebar-style = hidden


# --- Shell ---
command = /opt/homebrew/bin/nu

# Enable shell integration for better prompt/output handling
shell-integration = detect
osc-color-report-format = 8-bit

# Unbind any default keybinds you don't use
# keybind = clear

# --- Input & Behavior ---
macos-option-as-alt = true
mouse-hide-while-typing = true

# Entry point
# keybind = alt+v=activate_key_table:vim

# # Navigation bindings
# keybind = vim/j=scroll_page_lines:1
# keybind = vim/k=scroll_page_lines:-1
# keybind = vim/ctrl+d=scroll_page_down
# keybind = vim/ctrl+u=scroll_page_up
# keybind = vim/ctrl+f=scroll_page_down
#
#
# keybind = ctrl+j=scroll_page_lines:1
# keybind = ctrl+k=scroll_page_lines:-1
# keybind = ctrl+d=scroll_page_down
# keybind = ctrl+u=scroll_page_up
# keybind = ctrl+f=scroll_page_down
#
# --- Transitions ---
confirm-close-surface = false
# global-keybind = "cmd+shift+j=toggle_quick_terminal"

# Smoother rendering
window-vsync = true
# Better font rendering on macOS
font-thicken = true
cursor-style = bar
cursor-color = #f5e0dc

#url and link handling
# Click links with modifier
link-url = true
# Open links with specific browser (optional)
# macos-url-handler = /Applications/Arc.app

# Paste protection (warns on multi-line paste)
clipboard-paste-protection = true
clipboard-paste-bracketed-safe = true
# Trim trailing whitespace when copying
clipboard-trim-trailing-spaces = true
# When selecting text in fzf/nvim, auto-copy
copy-on-select = clipboard
clipboard-read = allow  
# Let tools like fzf read clipboard

# Let Zellij own ctrl+[key] combinations
# Ghostty handles cmd+[key] for window/tab management
keybind = cmd+t=new_tab
keybind = cmd+w=close_surface
keybind = cmd+1=goto_tab:1
keybind = cmd+2=goto_tab:2
keybind = cmd+3=goto_tab:3
keybind = cmd+4=goto_tab:4
keybind = cmd+5=goto_tab:5
keybind = cmd+6=goto_tab:6
keybind = cmd+7=goto_tab:7
keybind = cmd+8=goto_tab:8
keybind = cmd+9=goto_tab:9
# Quick access to config reload
keybind = cmd+shift+r=reload_config
# Don't let Ghostty intercept these - Zellij needs them
keybind = ctrl+a=unbind
keybind = ctrl+b=unbind

# Click behavior
mouse-shift-capture = true
# Scroll behavior
mouse-scroll-multiplier = 1.0

# Initial window size
window-height = 40
window-width = 120
# Save/restore window position
window-save-state = default
# Window title
window-title-font-family = "JetBrainsMono Nerd Font"

# Scrollback buffer (lines to remember)
scrollback-limit = 10000

# Better emoji rendering
grapheme-width-method = legacy

quick-terminal-position = top
quick-terminal-screen = main
quick-terminal-animation-duration = 0.2

# Ensure true color support
term = xterm-256color

# When focused on coding, hide distractions
unfocused-split-opacity = 0.6
focus-follows-mouse = false
# Prevent accidental focus changes

auto-update = download
# custom-shader = /Users/dan/.config/ghostty/shaders/ultra_minimal_cursor.glsl
# custom-shader = /Users/dan/.config/ghostty/shaders/cursor_for_the_weak.glsl 
# custom-shader = /Users/dan/.config/ghostty/shaders/kitty_optimised.glsl 
# custom-shader = /Users/dan/.config/ghostty/shaders/water.glsl
# custom-shader-animation = always
