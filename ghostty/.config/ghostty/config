
# --- Ghostty Configuration ---

# --- Fonts ---
font-family = "JetBrainsMono Nerd Font"
font-size = 22
font-feature = calt
font-feature = liga

# --- Theme & Colors ---
theme = catppuccin-macchiato
background-opacity = 0.85
background-blur-radius = 25

# --- Premium Aesthetic Tweaks ---
cursor-color = #f4dbd6
cursor-style = bar
# cursor-style-blink = false  <-- Causes error in v1.2.3, removing for stability.

# --- Window ---
window-padding-x = 12
window-padding-y = 12
window-decoration = true
# transparent is officially deprecated/buggy in some builds, but requested in ref.
# If tabs disappear, change this back to 'tabs'
macos-titlebar-style = transparent

# --- Shell ---
command = /bin/zsh

# --- Input & Behavior ---
macos-option-as-alt = true
copy-on-select = true
mouse-hide-while-typing = true

# --- Transitions ---
# Unsupported in v1.2.3 (Causes "unknown field" error)
# theme-transition-duration = 0.3s
confirm-close-surface = false
